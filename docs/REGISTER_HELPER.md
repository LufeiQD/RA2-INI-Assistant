# 注册表辅助功能说明

## ✨ 新增改进

### 1. 中文标签显示
CodeAction 提示时，注册表选项右侧显示中文说明，例如：
- `注册到 [Warheads] - 注册弹头`
- `注册到 [WeaponTypes] - 武器注册列表，注册没有被实际应用的武器`

### 2. 跨文件检测（包括 art 文件）
- 列出未注册节名时，会扫描所有已索引的 ini 文件
- 特别支持 art.ini、artmd.ini 等美术配置文件
- 如果其他文件已注册，则不会重复显示在列表中

### 3. 全局注册状态检测
- 考虑多个 ini 文件可能共用同一个注册列表的情况
- 例如：rules.ini 和 art.ini 都可能使用 [Animations] 注册表
- 只要任意文件已注册，就不会在补全列表中显示

## 🎯 使用示例

### 场景：定义新动画并注册

**1. 在 art.ini 中定义动画**
```ini
; 新的爆炸效果
[SuperExplosion]
Image=SUPEREXP
```

**2. 触发注册提示**
- 光标放在 `[SuperExplosion]` 上
- 按 `Ctrl+.` 或等待灯泡图标
- 选择：`注册到 [Animations] - 注册动画（开火动画弹头动画建筑工作动画等）`

**3. 自动生成注册代码**
如果 rules.ini 已有 [Animations] 节：
```ini
[Animations]
INVISO
INVULNEXP
+=SuperExplosion ; 新的爆炸效果
```

### 场景：在注册表内补全

**1. 在 rules.ini 中补全**
```ini
[Animations]
INVISO
+=█  ← 输入后触发补全
```

**2. 显示未注册的动画**
补全列表会显示：
- 当前 rules.ini 中定义的未注册动画
- art.ini / artmd.ini 中定义的未注册动画
- 排除已在任何文件中注册的动画

**3. 选择并补全**
```ini
[Animations]
INVISO
+=SuperExplosion
```

## 🔧 技术细节

### 跨文件检测机制
1. **利用索引管理器**：使用 `IniIndexManager.getRegisteredValues()` 获取全局已注册节名
2. **扫描所有文件**：通过 `indexManager.getAllSections()` 获取所有已定义的节
3. **智能过滤**：排除注册表节本身，排除已注册的节

### 注册状态判断
```typescript
// 全局已注册检测
const registeredGlobal = this.getRegisteredSectionsGlobal();
// 过滤未注册的节
unregistered = allSections.filter(s => !registeredGlobal.has(s.name));
```

### 中文标签映射
从 `translations.json` 的 `registerType` 数组读取：
```json
{
  "label": "注册弹头",
  "value": "[Warheads]"
}
```

## ⚙️ 配置要求

- **多文件搜索**：需启用 `ini-ra2.enableMultiFileSearch`（默认已启用）
- **相关文件白名单**：建议配置 `ini-ra2.relatedFiles` 包含：
  ```json
  [
    "rulesmd.ini",
    "artmd.ini",
    "rules.ini",
    "art.ini"
  ]
  ```

## 📊 性能优化

- 跨文件检测使用索引缓存，不会每次都扫描文件
- 注释读取采用异步加载，不阻塞补全显示
- 注册状态判断优先使用索引数据

---

**提示**：按 `F1` -> "Developer: Reload Window" 重新加载后生效。
