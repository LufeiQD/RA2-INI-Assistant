Action=SellUnit ；在TS里你可以把停在维修平台上的单位用变卖指令卖掉，在YR里如果你在超武上添加上面这个语句你也可以获得和TS那样的效果，但是存在问题，如果你卖掉坦克碉堡里的东西会导致IE，ARES修复了这个问题。不过我觉得这功能并没有什么卵用。

WaterImage= (VehicleType) ；在水中转换为这个单位的素材，参考武装直升机，只是借用壳子.需要注意的是shp单位并不支持这个逻辑。

EMPulseSparkles=EMP_FX01 ；EMP动画由[General]>EMPulseSparkles=EMP_FX01控制，现在每个单位可以自定义独立的EMP动画，弹头也可以设置其EMP效果。

EMP.Sparkles= (animation) ；当该单位被EMP时显示该动画。默认[General]?EMPulseSparkles

EMP.Sparkles= (animation) ；弹头的EMP效果，如果设置了该语句就会覆盖单位本身的EMP效果。只有在单位本身已经没有其他EMP效果时才会表现。

EMP.Duration= (integer - frames) ；被用来控制效果持续多长。每个单位都有个EMP计时器，这就是原理。如果你想让被EMP单位瘫痪十秒：EMP.Duration=150

EMP.Cap= (integer - frames) ； 如下表
-----------------------------------------------
EMP.Cap=0，还是EMP攻击，不过计数器无上限

EMP.Cap=-1 EMP将EMP计数器的数字立刻变成EMP.Duration的数字，除非本来的计数器已经比EMP.Duration大了

负数 EMP.Duration

EMP.Cap=-1
EMP计数器减少EMP.Duration的数字

EMP.Cap 大于0

EMP计数器按EMP.Duration减少，但是必然小于等于EMP.Cap，如果减少一次后仍大于，强制改写为EMP.Cap

EMP.Cap=0
立刻激活单位
----------------------------------------------

EMPAIRecoverMission= (mission) ；当AI的单位被EMP，解除后执行的动作。默认Hunt。

ImmuneToEMP= (boolean) ；是否免疫EMP

EMP.Modifier= (float - multiplier) ；如下表
----------------------------------------------
对EMP的抗性，只受到EMP影响的百分之几

以下建筑默认不免疫
Powered=yes 
Power= 负数

带以下标签的建筑默认不免疫
雷达
超武
变车辆
玩具坦克控制器
裂缝/隐形产生器
探测隐形单位
鸡光墙

 
步兵
除Cyborg=yes以外默认免疫 

车辆飞机
除Organic=yes外默认不免疫

人工设定的免疫会超越默认值生效

新的老兵精英升级包括EMP免疫：

VeteranAbilities=EMPIMMUNE
EliteAbilities=EMPIMMUNE
----------------------------------------------

EngineerCaptureLevel=(float - percent) ；多少血以下建筑才能一个工程师占领，在这之前工程师造成伤害

EngineerDamage= (float - percent) ；工程师占领前一个扣多少血

EngineerAlwaysCaptureTech= (boolean)  ；总是占领科技建筑，无视血量

EngineerDamageCursor.*=(cursor definition)  ；自定义鼠标动作

AllowMultiEngineer= (bool)  ；启用多位工程师勾选框

HasSpotlight= (boolean) ；是否使用探照灯，现在探照灯能够用于单位，建筑上的按原样扫描，车上的只能往前

Spotlight.StartHeight= (integer - leptons) ；探照灯射出的高度，默认430

Spotlight.Distance= (integer - leptons) ；探照灯射出的距离

Spotlight.AttachedTo= (enumeration - one of body|turret) ；从单位的哪个单位射出来，车体，炮塔或炮管？

Spotlight.DisableRed= (boolean) ；光照参数（不使用红色）其他：Spotlight.DisableGreen= (boolean) ；光照参数（不使用绿色）\n Spotlight.DisableBlue= (boolean) ；光照参数（不使用蓝色）

Spotlight.DisableGreen= (boolean) ；光照参数（不使用绿色）其他：Spotlight.DisableRed= (boolean) ；光照参数（不使用红色）\n Spotlight.DisableBlue= (boolean) ；光照参数（不使用蓝色）

Spotlight.DisableBlue= (boolean) ；光照参数（不使用蓝色）其他：Spotlight.DisableRed= (boolean) ；光照参数（不使用红色）\n Spotlight.DisableGreen= (boolean) ；光照参数（不使用绿色

Spotlight.DisableColor= (boolean) ；黑灯

LaserFencePost=yes ；激光栅栏现在能像TS里一样使用. 你需要一个建筑有LaserFencePost=yes语句来创建节点, 其他有LaserFence=yes语句的建筑之间会以激光连接（具体参考TS）。单位通过就会被摧毁。

LaserFence=yes ；激光栅栏现在能像TS里一样使用. 你需要一个建筑有LaserFencePost=yes语句来创建节点, 其他有LaserFence=yes语句的建筑之间会以激光连接（具体参考TS）。单位通过就会被摧毁。

Academy.InfantryVeterancy= (double - veterancy levels) ；拥有这样的建筑后步兵以及带有Organic=yes的车辆得到多少经验  \n 填写1.0即升为精英单位，填写2.0即升为老兵单位，0.0无效果。

Academy.AircraftVeterancy= (double - veterancy levels) ；拥有这样的建筑后空军以及带有ConsideredAircraft=yes的车辆得到多少经验  \n 填写1.0即升为精英单位，填写2.0即升为老兵单位，0.0无效果。

Academy.VehicleVeterancy= (double - veterancy levels) ；拥有这样的建筑后载具得到多少经验  \n 填写1.0即升为精英单位，填写2.0即升为老兵单位，0.0无效果。

Academy.BuildingVeterancy= (double - veterancy levels) ；拥有这样的建筑后建筑得到多少经验  \n 填写1.0即升为精英单位，填写2.0即升为老兵单位，0.0无效果。

Academy.Types= (list of TechnoTypes) ;这些单位会受到训练所的影响

Academy.Ignore= (list of TechnoTypes) ;这些单位不会受到训练所的影响


[ArmorTypes]  ；注册新护甲

Versus.magic= (float - modifier) ；该弹头对magic护甲的伤害百分比，因为WW弱智的拼写错误，现在旧弹头还是Verses=，不过ARES里少于11个也不IE，新弹头用Versus，需要注意下表
----------------------------------------------
弹头百分比特殊值
0%不主动不还击不响应强行攻击
1% 不还击不主动攻击
2% 不主动攻击
----------------------------------------------

Versus.shit.ForceFire= ；是否对shit护甲可以强行攻击

Versus.shit.Retaliate= ；是否可以对shit护甲反击

Versus.shit.PassiveAcquire= ；是否对shit护甲主动攻击

ImmuneToBerserk= (boolean)  ；决定一个单位是否免疫狂暴，即神经突击车的攻击效果。可以覆盖ImmuneToPsionics和psionicsimmune升级能力的效果。如果yes，即使ImmuneToPsionics=no也能免疫狂暴；如果no，单位一定能被混乱气体影响。默认ImmuneToPsionics或psionicsimmune升级能力 

Smoke.Anim= (animation) ；产生的尾烟。默认SGRYSMK1

Smoke.ChanceRed= (integer - percent) ；严重损伤时产生烟雾的密度（每帧），越大越多。默认10

Smoke.ChanceDead= (integer - percent) ；被击落时产生烟雾的密度（每帧），越大越多。默认80

AirRate= (integer - number of frames) ；如果该值大于零，规定了该单位在空中时，计划要给该单位几帧的动画，AirRate覆盖WalkRate与IdleRate，若该值为零，则禁用AirRate，默认为0

VoiceAirstrikeAttack= (soundmd entry) ；空袭单位进入地图时的语音。默认AirstrikeAttackVoice

VoiceAirstrikeAbort= (soundmd entry) ；空袭单位在开火前，指示单位被干掉时的语音。默认AirstrikeAbortSound

AlternateTheaterArt= (boolean) ；海豹的增强换装系统。例如，给单位设置Image=JUNK和AlternateTheaterArt=yes会使这个单位使用雪地装[JUNKA]，沙漠装[JUNKD] 等等，没有相应SHP默认[JUNK]，比原来的更灵活

AttachEffect.Animation= (AnimationType)；赋予属性的单位伴随的动画，就像伊文炸弹那样，动画要注册

AttachEffect.Duration= (integer)；属性持续时间，默认为0

AttachEffect.TemporalHidesAnim= (boolean)；被超时空武器攻击时是否停止动画？默认否

 

[Section]>AttachEffect.SpeedMultiplier= (float - multiplier)；速度加成

[Section]>AttachEffect.ArmorMultiplier= (float - multiplier)；装甲加成

[Section]>AttachEffect.FirepowerMultiplier= (float - multiplier)；伤害加成

[Section]>AttachEffect.ROFMultiplier= (double - multiplier) ；ROF的因数，就像国家设置里的那样。被影响的单位的攻击间隔会乘以这里指定的数值，默认1.0。\n 须注意，ROF数值并不是实时影响，仅在开火时赋予。因此如果一个单位处于该状态时开火，在状态结束后ROF不会对应变化。\n 比如设置AE效果ROF2倍，一个ROF为15的单位的ROF将会变为30，而开火后的瞬间AE效果消失了，但ROF仍然会按照30计算，不会对应缩短为15。

AttachEffect.Cloakable= (boolean) ；隐形加成

AttachEffect.ForceDecloak= (boolean) ；如果设置为是，那么使用该AE的单位会强制现行，默认否

AttachEffect.DiscardOnEntry= (boolean)；如果设置为是，那么当这个单位从地图上消失，比如进入建筑或单位里，AE效果会消失，默认否

AttachEffect.PenetratesIronCurtain= (boolean) ；决定该AE是否能够在目标单位/建筑上有铁幕或力盾时附加上，默认否。

AttachEffect.Delay= (integer)；该单位获得AE后多少时间内不能够再接收AE效果，负值为不重置效果。默认0，立刻马上

AttachEffect.InitialDelay= (integer - frames)；定义AE的初始间隔，即附着后多长时间生效。0代表立刻。默认0。

AttachEffect.Cumulative= (boolean) ；是否能够叠加作用，默认否

AttachEffect.AnimResetOnReapply= (boolean) ；如果不能叠加作用，再次赋予属性时会重设动画，默认否

Carryall.SizeLimit= (integer)；吊运规格的最大限制，默认为能够吊运任意单位即值为-1

Carryall.Allowed= (boolean)；这个载具能否吊运，Organic=yes和NonVehicle=yes的单位不能吊运. 如果指定那么覆盖全局，目前不可能“撤消”这个标签，所以必须指定这个标签，注意事项看下面
----------------------------------------------
蜘蛛什么的不能吊运
该单位要成功吊运必须要有AirportBound=no并且没有Dock=，否则不能吊运
建议不要吊运船，否则放不回水里，而扔到地上会爆掉，所以船尽量设置Carryall.Allowed=no
----------------------------------------------

ps：目前补充到吊运载具（补充时移除此句，同时帮忙的时候需要在最后添加上一句说明补充到哪里方便作者跟进）






















